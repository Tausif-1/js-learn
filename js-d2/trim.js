// trim - it is tuse to remove spaces.

let firstName = "  tausif   ";
console.log(firstName.length) // it shows 11 length.

firstName = firstName.trim() // it is use to remove spaces.
console.log(firstName)  // it removes spaces and print tausif.
console.log(firstName.length) // it again print 6 lenght.