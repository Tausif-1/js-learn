// slice :- it is use to slice the word .
// use of slice method -- any variable name .slice(start,end)

//ex:--

let firstName = "tausif";
firstName = firstName.slice(1,4);
console.log(firstName);

// it print and slice the word tausif and give aus as output.

// it print the aus as output and it exclude 4th index if we want to print 4th index we had to write this format (1,5) .

